const setPolotnoI18n = (i18n: any, setTranslations: any) => {
  const { removeBackgroundError } = i18n.error;
  const { reset } = i18n.scale;
  const {
    bodyText,
    createBody,
    createHeader,
    createSubHeader,
    error,
    headerText,
    height,
    layers,
    layersTip,
    magicResizeDescription,
    myFonts,
    namePlaceholder,
    noLayers,
    noResults,
    otherFormats,
    photos,
    searchTemplatesWithSameSize,
    subHeaderText,
    templates,
    uploadFont,
    uploadImage,
    uploadTip,
    useMagicResize,
    width,
    template,
    text,
    photo,
    elements,
    upload,
    background,
    resize,
    searchPlaceholder,
  } = i18n.sidePanel;
  const {
    alignBottom,
    alignCenter,
    alignLeft,
    alignMiddle,
    alignRight,
    alignTop,
    alwaysOnTop,
    blur,
    border,
    brightness,
    cancelRemoveBackground,
    confirmRemoveBackground,
    cornerRadius,
    crop,
    cropCancel,
    cropDone,
    down,
    download,
    duplicateElements,
    effects,
    fitToBackground,
    flip,
    flipHorizontally,
    flipVertically,
    grayscale,
    layering,
    lockedDescription,
    opacity,
    position,
    removeBackground,
    removeClip,
    removeElements,
    removeMask,
    saveAsImage,
    saveAsPDF,
    selectable,
    sepia,
    shadow,
    showInExport,
    textStroke,
    toBottom,
    toForward,
    transparency,
    unlockedDescription,
    up,
  } = i18n.toolbar;
  const { addPage, duplicatePage, moveDown, moveUp, noPages, removePage } =
    i18n.workspace;

  setTranslations({
    error: { removeBackground: removeBackgroundError },
    scale: { reset },
    sidePanel: {
      bodyText,
      createBody,
      createHeader,
      createSubHeader,
      error,
      headerText,
      height,
      layers,
      layersTip,
      magicResizeDescription,
      myFonts,
      namePlaceholder,
      noLayers,
      noResults,
      otherFormats,
      photos,
      searchTemplatesWithSameSize,
      subHeaderText,
      templates,
      uploadFont,
      uploadImage,
      uploadTip,
      useMagicResize,
      width,
      template,
      text,
      photo,
      elements,
      upload,
      background,
      resize,
      searchPlaceholder,
    },
    toolbar: {
      alignBottom,
      alignCenter,
      alignLeft,
      alignMiddle,
      alignRight,
      alignTop,
      alwaysOnTop,
      blur,
      border,
      brightness,
      cancelRemoveBackground,
      confirmRemoveBackground,
      cornerRadius,
      crop,
      cropCancel,
      cropDone,
      down,
      download,
      duplicateElements,
      effects,
      fitToBackground,
      flip,
      flipHorizontally,
      flipVertically,
      grayscale,
      layering,
      lockedDescription,
      opacity,
      position,
      removeBackground,
      removeClip,
      removeElements,
      removeMask,
      saveAsImage,
      saveAsPDF,
      selectable,
      sepia,
      shadow,
      showInExport,
      textStroke,
      toBottom,
      toForward,
      transparency,
      unlockedDescription,
      up,
    },
    workspace: {
      addPage,
      duplicatePage,
      moveDown,
      moveUp,
      noPages,
      removePage,
    },
  });
};

export default setPolotnoI18n;
